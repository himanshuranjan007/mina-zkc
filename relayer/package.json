{
  "name": "relayer",
  "version": "0.1.0",
  "description": "Bridge relayer service - watches Zcash and submits proofs to Mina",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "test": "bun test"
  },
  "dependencies": {
    "o1js": "^0.18.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0",
    "typescript": "^5.3.3"
  }
}

